#include "main.h"
/**
 * _isupper - checks if c is uppercase
 * @c: input for A - Z
 * Return: 1 if is uppercase and 0 otherwise
*/
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
{
	return (1);
}
	else
{
	return (0);
}
}		

