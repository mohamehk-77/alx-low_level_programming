#include <stdio.h>
/**
 * main - program start
 *
 * while function in charge
 *
 * Retrun: (scuccss) 0
*/
int main(void)
{
char ch = 'a'
while (ch <= 'z')
putchar(ch);
ch++;
putchar('\n');
return (0);
}
